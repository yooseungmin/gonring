--- a/haw/core/logger.py
+++ b/haw/core/logger.py
@@
-_PII_PATTERNS = [
-    r"\\d{6}-[1-4]\\d{6}",  # 주민등록번호
-    r"01[016789]-?\\d{3,4}-?\\d{4}",  # 휴대폰
-    r"[\\w.+-]+@[\\w.-]+\\.[A-Za-z]{2,}",  # 이메일
-    r"\\d{4}[-\\s]?\\d{4}[-\\s]?\\d{4}[-\\s]?\\d{4}",  # 카드번호
-    r"C:\\\\Users\\\\[^\\\\]+\\\\",  # Windows 사용자 경로
-]
+_PII_PATTERNS = [
+    r"\d{6}-[1-4]\d{6}",             # 주민등록번호
+    r"01[016789]-?\d{3,4}-?\d{4}",   # 휴대폰
+    r"[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}",  # 이메일
+    r"\d{4}[-\s]?\d{4}[-\s]?\d{4}[-\s]?\d{4}",  # 카드번호
+    r"C:\\Users\\[^\\]+\\",           # Windows 사용자 경로
+]
@@
-def _redact_record(record):
+def _redact_and_allow(record) -> bool:
     record["message"] = _redact_all(str(record.get("message", "")))
-    return record
+    return True
@@
-            patcher=_redact_record,
+            filter=_redact_and_allow,
